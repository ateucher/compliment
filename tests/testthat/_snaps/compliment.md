# invalid n errors

    Code
      compliment(0)
    Condition
      Error:
      ! `n` must be a single positive number.

---

    Code
      compliment("a")
    Condition
      Error:
      ! `n` must be a single positive number.

